package com.home.timetracking
import okhttp3.OkHttpClient
import okhttp3.Request

class Sync {
    fun syncSession(session: Session) {
        val client = OkHttpClient()

//        val reequest = Request.Builder().url("192.168.1.21").post()
    }
}